'use strict';

module.exports = {
    constructor: Array,
    every: require('./every'),
    fill: require('./fill'),
    filter: require('./filter'),
    forEach: require('./forEach'),
    map: require('./map'),
    reduce: require('./reduce'),
    reverse: require('./reverse'),
    some: require('./some')
};
