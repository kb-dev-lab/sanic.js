'use strict';

module.exports = {
    every: require('./every'),
    filter: require('./filter'),
    forEach: require('./forEach'),
    map: require('./map'),
    reduce: require('./reduce'),
    some: require('./some')
};